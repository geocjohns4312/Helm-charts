apiVersion: v1
kind: Service
metadata:
  name: cp-jenkins
  labels:
    app: cp-jenkins
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
  selector:
    app: cp-jenkins
